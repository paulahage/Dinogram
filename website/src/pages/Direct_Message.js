import React from "react";
import styled from "styled-components";

const DirectMessage = () => {
  return <DmWrapper>Direct_Message</DmWrapper>;
};

export default DirectMessage;

const DmWrapper = styled.div`
  text-align: center;
`;
